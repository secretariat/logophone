<article class="element fx-slide-up home clearfix one-full about isotope-item">
  <div class="reveal-bg-nine">
    <center><h2 class="pad pad-top-minus pad-bottom-minus">Saved contacts</h2></center>
  </div>
</article>
<% i = 0 %>
<% @logos.each do |logo| %>
  <article class="element fx-slide-out clearfix one-third home overflow" style="height:450px;">
    <!-- <a href="images/content/01.jpg" title="" class="popup"> -->
    <div class="images">
        <div class="layerscab">
          <% layer = 1 %>
          <% logo.logo_files.split(',').each do |file| %>
              <% if file =~ /1\w{7}\.png/%>
                <%= image_tag file, :class => "m1m2 imgl"+layer.to_s, :width => "225", :alt => "logophone"%>
              <% elsif file =~ /5\w{7}\.png/ %>
                <%= image_tag file, :class => "m1m3 imgl"+layer.to_s, :width => "270", :alt => "logophone"%>
              <% elsif file =~ /flag/ %>
                <%= image_tag file, :class => "m1mf imgl"+layer.to_s, :alt => "logophone"%>
              <% else %>
                <%= image_tag file, :width => "300", :class => "imgl"+layer.to_s, :alt => "logophone"%>
              <% end %>
            <% layer +=1 %>
          <% end %>
        </div>
    </div>

    <center>
      <%#= link_to( "Delete", creator_destroy_path(:id => logo.id), :method => :delete,  :class => "icon-remove", :confirm => "Вы уверены, что хотите удалить #{logo.first_name} #{logo.last_name}?") %>
    </center>

    <div class="inner-panel reveal-bg-<%= @colors[i] %>">
      <div class="thumb pad pad-bottom-minus" style="padding:0;"></div>
      <center>
        <% if logo.first_name != "" && logo.first_name != ""%>
          <h2 class="pad pad-top-minus mh2"><%= logo.first_name %> <%= logo.last_name %></h2>
          <h5 class="mp0"><%=logo.phone %></h5>
        <% else  %>
          <h2 class="pad pad-top-minus mh2"><%=logo.phone %></h2><h5 class="mp0">&nbsp</h5>
        <% end %>
      </h2></center>
    </div>
    </a>
  </article>
  <% i += 1 %>
  <% i = 1 if i == 8 %>
<% end %>